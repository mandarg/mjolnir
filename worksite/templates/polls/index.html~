{% if latest_poll_list %}
    <ul>
    {% for poll in latest_poll_list %}
    <li><a href="/work_survey/{{ poll.id }}/">{{ poll.question }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No surveys are available.</p>
{% endif %}
